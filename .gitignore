/backend/storage

# Dependências npm (nunca commitar).
node_modules/

# Diretórios de scratch / artefactos de smoke test (não devem ir para o repo).
/tmp/

# Variáveis de ambiente — nunca commitar (segredos).
.env
.env.local
.env.*.local
backend/.env
backend/.env.*
frontend/.env.local
frontend/.env.*.local
