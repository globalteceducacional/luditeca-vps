/**
 * Classes partilhadas do shell CMS (Argon / Luditeca).
 * O visual base aplica-se sob `.argon-page-transition` em `argon-luditeca.css`;
 * use estas constantes quando precisar de comportamento extra (ex.: hover só na grelha).
 */
export const CMS_SHELL_CLASS = 'luditeca-cms-shell';

/** Cartão de livro na grelha — hover elevado (CSS em argon-luditeca.css) */
export const CMS_BOOK_CARD_CLASS = 'shadow border-0 card-profile h-100';
